<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <import type="android.view.View" />

        <variable
            name="productViewModel"
            type="com.mohannad.coupon.view.ui.product.ProductsViewModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/ly_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".view.ui.product.ProductsActivity">

        <com.facebook.shimmer.ShimmerFrameLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:visibility="@{safeUnbox(productViewModel.dataLoading) ?  View.VISIBLE : View.GONE}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:paddingTop="8dp">

                <include layout="@layout/shimmer_place_holder_top_product_layout" />

                <include layout="@layout/shimmer_place_holder_top_product_layout" />

                <include layout="@layout/shimmer_place_holder_top_product_layout" />

                <include layout="@layout/shimmer_place_holder_top_product_layout" />

                <include layout="@layout/shimmer_place_holder_top_product_layout" />

                <include layout="@layout/shimmer_place_holder_top_product_layout" />
            </LinearLayout>
        </com.facebook.shimmer.ShimmerFrameLayout>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_products_activity_products"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:visibility="@{safeUnbox(productViewModel.dataLoading) ? View.GONE: View.VISIBLE}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:listitem="@layout/item_product_rv" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>